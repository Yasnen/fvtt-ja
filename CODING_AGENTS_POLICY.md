# コーディングエージェントのプロジェクト方針

## プロジェクト情報

| 項目 | 値 |
|---|---|
| プロジェクト名 | fvtt-ja |
| 対象ブランチ | `main`（FVTT v14）、`v13`（保守） |
| 記入日 | 2026-08-20 |
| Claude Code | Claude Pro |
| Codex | ChatGPT Plus |
| 共通指示 | `AGENTS.md` |
| 必須検証 | JavaScript構文、JSON構文 |

## 採用方針

| 項目 | 方針 |
|---|---|
| 利用ベンダー | AnthropicとOpenAIの複数社 |
| データ送信 | 処理に必要なリポジトリ内容だけを送信する |
| 学習・第三者提供 | 両アカウントでモデル改善を無効化し、第三者への販売・目的外提供を許可しない |
| ベンダーへ渡さない範囲 | 認証情報、秘密鍵、トークン、端末固有情報、第三者の非公開データ |
| 共通規則 | `AGENTS.md`へ一本化する |
| Claude Code設定 | `.claude/`を端末ローカル・Git管理外とする |
| Claude Code sandbox | sandbox内Bashの自動許可を維持し、sandbox外実行と起動失敗はfail-closedとする |
| Codex設定 | `.codex/config.toml`をGit共有し、`workspace-write`を使用する |
| 外部ネットワーク | 公開情報の取得は許可し、認証や外部状態変更は承認対象とする |
| Web検索 | 情報収集目的で許可し、FVTT公式APIなど一次資料を優先する |
| 履歴取り込み | クロスベンダーの履歴取り込みは実施しない |
| 非対話経路 | 現時点では採用しない。必要時に経路単位で検証する |
| エージェント併用 | 同じworktreeでは交互操作とする |
| コミット | ユーザーの明示的な依頼がある場合だけ実行する |
| push・タグ・リリース | 自動実行しない |

## データと生成物

- リポジトリ内の公開コード、文書、翻訳データはAI支援の対象にできる。
- 認証情報、利用者のローカルデータ、未公開の第三者データを無断で外部送信しない。
- 翻訳テストには著作権上問題のない最小の人工データを使う。
- AI支援コードは、作者が理解、説明、修正、保守できる状態でのみ採用する。
- `main`と`v13`の変更を混同せず、backportは個別に判断する。

## 検証項目

- [x] Claude Codeで`CLAUDE.md`から`AGENTS.md`が読み込まれる
- [x] Codexで`AGENTS.md`と`.codex/config.toml`が読み込まれる
- [x] Claude CodeとCodexのsandbox・approvalを確認する
- [x] ワークスペース内外の書き込み境界を確認する
- [x] コマンド通信と組み込みWeb検索を別々に確認する
- [x] JavaScriptとJSONの構文検証が成功する
- [x] 共有ファイルに認証情報・端末固有パスがない

## 承認結果の確認

承認UIやエージェントの自己申告だけを結果とせず、書き込み拒否後は対象パスの実在を独立したコマンドまたは別セッションで確認する。画面上の`Ran`、承認済み、削除済みという表示とファイルシステムの状態が食い違う場合は、ファイルシステム上の結果を判定根拠とする。

## ネットワーク経路

Codexではshell通信がsandbox内のDNS制限で失敗し、sandbox外実行の承認後に成功した。組み込みWeb検索は承認なしで成功した。Claude CodeではBashの公開HTTPS通信とWebSearch／WebFetchの双方が承認なしで成功した。コマンド通信と組み込みWeb機能は別の権限経路として扱い、公開情報の検索許可を外部状態変更の許可へ拡張しない。

## 再検証

Claude Code、Codex、sandbox設定、FVTT対象メジャー、ブランチ運用、検証手順を変更した場合は、対応する項目を再検証する。
